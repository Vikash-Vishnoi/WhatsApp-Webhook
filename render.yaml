services:
  - type: web
    name: whatsapp-webhook
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: MONGODB_URI
        sync: false
      - key: WHATSAPP_VERIFY_TOKEN
        value: hospital_whatsapp_2024
      - key: BACKEND_URL
        sync: false
      - key: WEBHOOK_SECRET
        sync: false
