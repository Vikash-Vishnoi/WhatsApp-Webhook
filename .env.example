# WhatsApp Webhook Configuration

# MongoDB Connection (Required)
# Get this from MongoDB Atlas or use local MongoDB
MONGODB_URI=mongodb://localhost:27017/whatsapp-marketing

# WhatsApp Verify Token (Required)
# This must match the token you set in Meta's Webhook Configuration
WHATSAPP_VERIFY_TOKEN=hospital_whatsapp_2024

# Backend Server URL (Optional)
# URL of your main backend server for real-time notifications
BACKEND_URL=http://localhost:5000

# Webhook Secret (Optional)
# Secret key for securing webhook notifications to your backend
WEBHOOK_SECRET=your-secret-key-here

# Server Port (Optional)
# Port for the webhook server (Render.com will set this automatically)
PORT=3000

# Node Environment
NODE_ENV=production
